[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
markers = [
    "covers(*req_ids): link this test to one or more spec requirement IDs (e.g. R3.1) for the traceability matrix",
]
